#include "world.h"

// Constructor
World::World() {}

// Gets the height at (x, z)
int World::getHeight(float x, float z) const {
    float height = 128.0f;

    // TODO

    return static_cast<int>(height);
}
